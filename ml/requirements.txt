# ═══════════════════════════════════════════════════════════════
#  InSilico QSPR Engine — Python Dependencies
# ═══════════════════════════════════════════════════════════════
#
#  Core:     pip install -r requirements.txt
#  With HPO: pip install -r requirements.txt optuna
#
# ═══════════════════════════════════════════════════════════════

# ── Web Framework ──
flask>=3.0
flask-cors>=4.0

# ── Chemistry ──
rdkit                  # SMILES parsing, Morgan FP, molecular descriptors

# ── ML Models ──
xgboost>=2.0           # Gradient boosted trees
scikit-learn>=1.4       # RandomForest, StandardScaler, metrics

# ── Data ──
pandas>=2.1
numpy>=1.26
joblib>=1.3

# ── Hyperparameter Optimization (optional but recommended) ──
optuna>=3.5            # Bayesian HPO with TPE sampler

# ── Environment ──
python-dotenv>=1.0     # Load .env.local for API keys

# ── PersonaPlex Voice AI ──
requests>=2.31         # Cerebras AI HTTP client (Python bridge)
edge-tts>=7.0          # Microsoft Edge Neural TTS (free, high-quality)
# nvidia-riva-client    # NVIDIA Riva ASR/TTS (optional, install if Riva server available)
