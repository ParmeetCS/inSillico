# ═══════════════════════════════════════════════════════════════
#  InSilico ML — SLIM deployment dependencies (Render 512MB)
# ═══════════════════════════════════════════════════════════════
# Removed: optuna, edge-tts, nvidia-riva-client (not needed for serving)

# ── Web Framework ──
flask>=3.0
flask-cors>=4.0

# ── Chemistry ──
rdkit

# ── ML Models (inference only) ──
xgboost>=2.0
scikit-learn>=1.4

# ── Data ──
pandas>=2.1
numpy>=1.26
joblib>=1.3

# ── Environment ──
python-dotenv>=1.0

# ── HTTP client for Cerebras ──
requests>=2.31
